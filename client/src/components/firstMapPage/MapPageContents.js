import React            from 'react';



const MapPageContents = (props) => {
    return (
        <>
            {/* all the contents go here as tables */}
        </>
    );
};

export default MapPageContents;
